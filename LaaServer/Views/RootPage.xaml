<Page x:Class="LaaServer.Views.RootPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LaaServer.Views"
      xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
      xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"           
      xmlns:viewModels="clr-namespace:LaaServer.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Style="{DynamicResource MahApps.Styles.Page}"
      Title="Main Page">
    <Page.DataContext>
        <viewModels:RootViewModel />
    </Page.DataContext>
    <Grid>
        <StackPanel Width="370" Orientation="Vertical" VerticalAlignment="Center">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                <Button Content="Bluetooth" Width="140" Margin="0,0,0,15" Command="{Binding BthButtonCommand}" />
                <Button Content="Wifi" Width="140" Command="{Binding WifiButtonCommand}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
